const CONFIG = {
    BASE_URL: "https://petstore.swagger.io/v2",
    API_TOKEN: process.env.PETSTORE_API_TOKEN || 'test123*', // You can add more configuration settings as needed
  };
  
  export default CONFIG;
  