// config.ts

const BASE_URL = 'http://www.uitestingplayground.com';

const config = {
  urls: {
    base: BASE_URL,
    textInput: `${BASE_URL}/textinput`,
    dynamicTable: `${BASE_URL}/dynamictable`,
    ajax: `${BASE_URL}/ajax`
  }
};

export default config;